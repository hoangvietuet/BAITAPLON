#include "Properties.h"

// Constructor của lớp Properties
Properties::Properties(Vector2D pos, const std::string& texture)
    : position(pos), textureID(texture) {
    // Không có gì đặc biệt để làm ở đây, chỉ khởi tạo các thuộc tính từ tham số đầu vào
}
